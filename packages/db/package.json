{
  "name": "@study/db",
  "version": "0.1.0",
  "private": true,
  "description": "Supabase clients, generated database types, and SQL migrations for Alex's Study Dashboard",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:reset": "supabase db reset",
    "db:push": "supabase db push",
    "db:migration": "supabase migration new",
    "db:types": "supabase gen types typescript --local --schema public > src/types/database.ts",
    "db:types:remote": "supabase gen types typescript --linked --schema public > src/types/database.ts"
  },
  "dependencies": {
    "@supabase/ssr": "catalog:",
    "@supabase/supabase-js": "catalog:"
  },
  "devDependencies": {
    "@study/typescript-config": "workspace:*",
    "typescript": "catalog:"
  }
}
