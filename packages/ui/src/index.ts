/**
 * Shared UI components live here once a component is needed by more than
 * apps/web (e.g. a future mobile app or marketing site). Until then,
 * app-specific components belong in apps/web/src/components.
 */
export {};
